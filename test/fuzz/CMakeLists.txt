add_executable(fuzz fuzz.cpp)
target_link_libraries(fuzz "${CUB_NAME}" test_tools pthread)
target_include_directories(fuzz PRIVATE "${CUB_SOURCE_DIR}")
target_include_directories(fuzz PUBLIC "${CUB_INCLUDE_DIR}")
target_compile_options(fuzz PRIVATE "-fsanitize=address,fuzzer")
target_link_options(fuzz PRIVATE "-fsanitize=address,fuzzer")
